#ifndef AGGREGATION_H // To make sure you don't declare the function more than once by including the header multiple times.
#define AGGREGATION_H

#include <unordered_map>
#include <vector>
#include <string>

void passRateByInstructor();

#endif